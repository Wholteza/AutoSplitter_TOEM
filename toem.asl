state("TOEM") {
    // TODO check for area
}

init {
    // called when the script connects to a game process
}

startup {
    // runs when the script is first loaded
}

split {
    if (
        // TODO enter new area detected
    ) {
        return true;
    }
}